# CSV Viewer - ユーザーガイド 📖

CSV Viewerは、CSVファイルを美しく表示・分析するための無料Webアプリケーションです。

---

## 🚀 はじめに

### アクセス方法
ブラウザで https://csv-viewer-app.vercel.app にアクセスします。

### 対応ブラウザ
- Google Chrome (推奨)
- Microsoft Edge
- Firefox
- Safari

---

## 📂 CSVファイルのアップロード

### 方法1: ファイル選択ボタン
1. 「**CSVファイルを選択**」ボタンをクリック
2. パソコンからCSVファイルを選択
3. 自動的にデータが表示されます

### 方法2: ドラッグ&ドロップ
1. CSVファイルをブラウザの画面にドラッグ
2. ドロップゾーンが青くハイライトされたらドロップ
3. 自動的にデータが表示されます

### 方法3: キーボードショートカット
- **Ctrl + O** (Mac: Cmd + O) でファイル選択ダイアログを開く

---

## ⚠️ ファイルの注意事項

### ファイル要件
| 項目 | 要件 |
|------|------|
| **ファイル形式** | `.csv` のみ |
| **最大サイズ** | 50MB |
| **文字コード** | UTF-8 (推奨) |
| **最大行数** | 5,000行 (それ以上は最初の5,000行のみ表示) |

### エラーメッセージの例
```
❌ ファイルサイズが大きすぎます（最大: 50MB）
❌ CSVファイル（.csv）を選択してください
❌ ファイルが空です
```

エラーメッセージは右上の「×」ボタンで閉じられます。

---

## 🎯 基本機能

### 1. データ表示モード

#### テーブルモード
- スプレッドシートのように表形式で表示
- 行の背景色が交互に変わり見やすい
- 列ヘッダーをクリックでソート可能

#### JSONモード
- JSON形式でデータを表示
- プログラマー向けの表示形式

**切り替え方法**: 画面上部の「**テーブル**」「**JSON**」ボタンをクリック

---

### 2. データ検索 🔍

#### 検索方法
1. 検索ボックスにキーワードを入力
2. 検索対象を選択:
   - 「すべての列」: 全列から検索
   - 特定の列名: その列のみ検索

#### キーボードショートカット
- **Ctrl + F** (Mac: Cmd + F) で検索ボックスにフォーカス

#### 検索結果
- マッチした行のみが表示されます
- ページは自動的に1ページ目に戻ります
- 検索結果の件数が表示されます

---

### 3. 列の管理

#### 列の表示/非表示
1. 「**列の設定**」ボタンをクリック
2. チェックボックスで列の表示/非表示を切り替え
3. 「すべて表示」「すべて非表示」ボタンも使用可能

#### 列の並べ替え
- 列ヘッダーの左側にある「≡」アイコンをドラッグ&ドロップ
- 好きな順序に並べ替え可能

#### 列幅の調整
1. 列ヘッダーの右端にカーソルを合わせる
2. カーソルが「⟷」に変わったらドラッグ
3. 好みの幅に調整

#### 列幅のリセット
- 「**リセット**」ボタンで全列の幅を初期状態に戻す

---

### 4. データのソート

#### ソート方法
1. 列ヘッダーをクリック
2. もう一度クリックで昇順⇔降順を切り替え
3. ソート中の列には「▲」または「▼」が表示

#### ソートの仕様
- **数値列**: 数値として正しくソート
- **文字列列**: アルファベット順/五十音順
- **空欄**: 最後に表示

---

### 5. ページネーション

#### ページ移動方法
- **ボタン**: `«` `‹` `›` `»` ボタンをクリック
  - `«`: 最初のページ
  - `‹`: 前のページ
  - `›`: 次のページ
  - `»`: 最後のページ
- **キーボード**: `←` `→` キーで前後のページに移動

#### 表示件数の変更
1. 「表示件数」ドロップダウンを選択
2. 10件/25件/50件/100件から選択
3. 「全件表示」チェックボックスで全データを表示

---

### 6. データのエクスポート 📥

#### JSON形式でエクスポート
- **ボタン**: 「**JSON保存**」ボタンをクリック
- **ショートカット**: Ctrl + E (Mac: Cmd + E)
- ファイル名: `[元のファイル名]_export.json`

#### CSV形式でエクスポート
- **ボタン**: 「**CSV保存**」ボタンをクリック
- **ショートカット**: Ctrl + S (Mac: Cmd + S)
- ファイル名: `[元のファイル名]_export.csv`

#### JSONをクリップボードにコピー
- JSONモードで「**JSONコピー**」ボタンをクリック
- 現在のページのデータがコピーされます

---

### 7. 表示オプション

#### セル表示モード
- **1行（折り返しなし）**: 長いテキストを1行で表示
- **1行（省略表示）**: 長いテキストを「...」で省略

#### ヘッダー折り返し
- スイッチで列名の折り返しを制御
- オフ: 1行表示
- オン: 複数行で表示

#### フルスクリーンモード
- 「**フル画面**」ボタンでテーブルを全画面表示
- 「**×**」ボタンまたは「**解除**」ボタンで終了

---

## ⌨️ キーボードショートカット

すべてのショートカットは画面下部の「キーボードショートカット」セクションで確認できます。

| ショートカット | 機能 |
|---------------|------|
| **Ctrl/Cmd + O** | ファイルを開く |
| **Ctrl/Cmd + F** | 検索ボックスにフォーカス |
| **Ctrl/Cmd + E** | JSONエクスポート |
| **Ctrl/Cmd + S** | CSVエクスポート |
| **Ctrl/Cmd + K** | サンプルデータを表示 |
| **← →** | ページ移動（前/次） |
| **Esc** | エラーメッセージを閉じる |

---

## 🎨 その他の機能

### サンプルデータ
- 「**サンプルデータを表示**」ボタンでデモデータを読み込み
- アプリの機能を試すのに最適

### ツールチップ
- セルの上に2秒間マウスを置くと、セルの内容全体がツールチップで表示されます
- 長いテキストの確認に便利

### データ統計
- 総レコード数が画面上部に表示
- 検索時は検索結果の件数も表示

---

## ❓ よくある質問 (FAQ)

### Q1. どのようなCSVファイルに対応していますか？
**A:** UTF-8エンコードの標準的なCSVファイル（カンマ区切り）に対応しています。Excel等で保存する際は「CSV UTF-8」形式を選択してください。

### Q2. ファイルサイズの制限はありますか？
**A:** 最大50MBまで対応しています。また、パフォーマンスのため最初の5,000行のみ表示されます。

### Q3. データはサーバーに保存されますか？
**A:** いいえ、すべてブラウザ内で処理されます。データが外部に送信されることはありません。

### Q4. Excelファイルは開けますか？
**A:** 直接は開けません。ExcelでCSV形式（UTF-8）として保存してからアップロードしてください。

### Q5. 日本語のファイルは使えますか？
**A:** はい、UTF-8エンコーディングであれば問題なく表示できます。

### Q6. 列の設定は保存されますか？
**A:** はい、列の表示/非表示、列の順序、列幅はブラウザのローカルストレージに保存されます。

### Q7. スマートフォンでも使えますか？
**A:** 基本的な機能は使えますが、PC/タブレットでの使用を推奨します。

### Q8. 文字化けします
**A:** ファイルがUTF-8でエンコードされているか確認してください。Shift-JISやEUC-JPは非対応です。

---

## 🛠️ トラブルシューティング

### ファイルがアップロードできない
1. ファイル形式が `.csv` か確認
2. ファイルサイズが50MB以下か確認
3. ファイルが空でないか確認
4. 別のブラウザで試す

### データが正しく表示されない
1. CSVファイルがカンマ区切りか確認
2. UTF-8エンコーディングか確認
3. 列名（ヘッダー行）が存在するか確認

### 検索が動作しない
1. データが読み込まれているか確認
2. 検索キーワードの全角/半角を確認
3. 大文字/小文字は区別されません

### エラーメッセージが消えない
- 右上の「×」ボタンをクリック
- または **Esc** キーを押す

---

## 📞 サポート

### 問題が解決しない場合
GitHubの[Issues](https://github.com/BoxPistols/csv-viewer-app/issues)でお問い合わせください。

### バグ報告
以下の情報を含めてご報告ください：
- 使用ブラウザとバージョン
- エラーメッセージ（スクリーンショット推奨）
- 再現手順

---

## 🎉 楽しいCSVライフを！

CSV Viewerを使って、データ分析を快適に行ってください！
